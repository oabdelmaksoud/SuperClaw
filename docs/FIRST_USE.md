# FIRST_USE

1. Ensure skills are installed in `~/.openclaw/skills`.
2. Start a new session (`/new`) to refresh skill list.
3. Try a first task: "Plan a feature rollout and wait for confirmation."
4. Run smoke checks in a repo:
   - `bash scripts/run-verify.sh quick .`
   - `bash scripts/run-eval.sh .`
