# MCP_COMPATIBILITY

Profiles:
- minimal: memory only
- standard: memory + github + filesystem
- strict: memory only + explicit least-privilege guidance

Use placeholders for secrets and set them in environment variables.
